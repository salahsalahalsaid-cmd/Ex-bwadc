-- ============================================================
-- مثال توضيحي فقط: إزاي تضيف "ترقية" جديدة لقاعدة البيانات مستقبلًا.
-- ده ملف اختياري - سيب زي ما هو أو احذفه، مش لازم لتشغيل الموقع.
--
-- الفكرة: كل مرة تحتاج تعدّل في قاعدة البيانات (تضيف عمود، جدول،
-- فهرس...الخ) اعمل ملف جديد هنا بنفس الأسلوب برقم أكبر من اللي قبله
-- (003_..., 004_...) وشغّل: npm run migrate
-- ============================================================

ALTER TABLE news ADD COLUMN IF NOT EXISTS views_count INTEGER NOT NULL DEFAULT 0;
